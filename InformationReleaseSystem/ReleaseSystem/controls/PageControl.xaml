<UserControl x:Class="ReleaseSystem.controls.PageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
              d:DesignHeight="80" d:DesignWidth="500">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition  Width="*"></ColumnDefinition>
            <ColumnDefinition  Width="100"></ColumnDefinition>

        </Grid.ColumnDefinitions>
        <Button x:Name="btnLast" Style="{StaticResource btnLastNext}" Click="btnLast_Click" Grid.Column="0" Width="35"  Content="&lt;&lt;" Height="35"></Button>
        <StackPanel Orientation="Horizontal" Grid.Column="1" x:Name="spPages"></StackPanel>
        <Button  x:Name="btnNext" Style="{StaticResource btnLastNext}" Click="btnNext_Click" Grid.Column="2" Content=">>"  Width="35"  Height="35"></Button>
    </Grid>
</UserControl>
